pydantic>=2.5.0
pyyaml>=6.0.1
python-dotenv>=1.0.0
requests>=2.31.0
eth-account>=0.11.0
rapidfuzz>=3.0.0
py-clob-client>=0.15.0
websockets>=12.0
flask>=3.0.0
pytest>=8.0.0
numpy>=1.24  # MLB submarket probability vectors (SPEC-R Plan 2)
pybaseball>=2.2  # Statcast pitch-by-pitch event data (SPEC-R Plan 3 T2)
# Basketball — NBA + WNBA stats (birincil veri kaynağı)
# nba_api: stats.nba.com wrapper, ESPN'e göre daha zengin advanced stats sağlar
nba_api>=1.11.0,<2.0
# Euroleague (Faz 3) — Avrupa basket lig stats
# euroleague-api (giasemidis): PyPI sustainable, FIBA possessions 0.46
# Opsiyonel: paket yoksa basketball Euroleague refresh hook'u degrade boş döner
euroleague-api>=0.0.13
# Avrupa yerel basket ligleri — BSL (Türkiye), ACB (İspanya), Lega (İtalya)
# Opsiyonel: paket yoksa fetcher None → BRScraper hook degrade boş döner (NO TRADE)
BRScraper>=0.1.0
